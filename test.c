#include "types.h"
#include "user.h"
#include "stat.h"

int main(int argc, char const *argv[])
{

    while (1)
    {
        int size = test(-1, "Hi");
        // top();
        sleep(100);
    }
    // printf(1, "%d\n", r);
    exit();
}